#include "conviqt.hpp"

// This file will contain the I/O facilities: read beam, read sky, read parameters

// An end user will have the choice between using these facilities or providing inputs directly

int beam::read_beam( std::string fname ) {
  return 0;
}

int sky::read_sky( std::string fname ) {
  return 0;
}
