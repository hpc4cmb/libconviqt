
#include "conviqt.hpp"

// This file will contain the actual operations on the sky, no I/O

convolver::convolver( sky s, beam b ) : s(s), b(b) { }

int convolver::set_sky( sky s ) {
  return 0;
}

int convolver::set_beam( beam b ) {
  return 0;
}

int convolver::convolve( pointing pnt ) {
  return 0;
}
